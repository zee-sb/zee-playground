node_modules/
.claude/
.vercel
.env
.env.local
.env*.local

# Working artifacts produced during exploration / live-MCP probes (decks,
# md drafts, JSON dumps). Keep them on disk locally but out of git.
outputs/
*.docx
